# Task Breakdown: Improve Login

## Overview

Total Tasks: 2

## Task List

### Backend Layer

#### Task Group 1: Session Persistence

**Dependencies:** None

- [x] 1.0 Fix session timeout issue
  - [x] 1.1 Write 2 focused tests for Auth JWT configuration
    - Create/Update `apps/server/src/modules/auth/auth.module.spec.ts` (if exists or create new test file)
    - Test that JWT module config produces a token with 7d expiration (mock ConfigService if needed)
    - Skip exhaustive auth flow testing, focus on config
  - [x] 1.2 Update JWT expiration configuration
    - Modify `apps/server/src/modules/auth/auth.module.ts`
    - Change `expiresIn` from `15m` to `7d`
  - [x] 1.3 Ensure backend tests pass
    - Run ONLY the focused tests for auth module config
    - Verify token expiration is correctly set

**Acceptance Criteria:**

- JWT tokens generated by the server have a 7-day expiration claim (`exp`).
- User stays logged in after 15 minutes.

### Frontend Layer

#### Task Group 2: UI Optimization & Localization

**Dependencies:** None (can run parallel to Backend)

- [x] 2.0 overhaul Login UI
  - [x] 2.1 Write 2-4 focused tests for Login component
    - Update/Create `apps/client/src/pages/Login.test.tsx`
    - Test that "Iniciar Sesión" button exists (Spanish)
    - Test that input fields are present with correct Spanish labels
    - Test that error message appears when login fails
  - [x] 2.2 Localize Login strings to Spanish
    - Update `apps/client/src/pages/Login.tsx`
    - "Sign In" -> "Iniciar Sesión"
    - "Email" -> "Correo Electrónico"
    - "Password" -> "Contraseña"
    - "Don't have an account? Sign up" -> "¿No tienes cuenta? Regístrate"
  - [x] 2.3 Optimize UI for Tablet (Touch Targets)
    - Update `apps/client/src/pages/Login.tsx`
    - Use `h-12` (48px) for all Inputs and Buttons
    - Increase font size to `text-lg` for inputs and buttons
    - Center the card properly for iPad viewport
  - [x] 2.4 Improve Error Handling
    - Update `apps/client/src/pages/Login.tsx`
    - Display "Correo o contraseña incorrectos" on 401/403 errors
    - Style error message as a distinct red alert box
  - [x] 2.5 Ensure Frontend tests pass
    - Run ONLY the tests in `apps/client/src/pages/Login.test.tsx`
    - Verify Spanish text and error handling logic

**Acceptance Criteria:**

- Login screen is fully in Spanish.
- Inputs and Buttons are at least 48px tall.
- Error messages are friendly and in Spanish.
- Tests pass.

## Execution Order

Recommended implementation sequence:

1. Backend (Task Group 1) - Quick config fix
2. Frontend (Task Group 2) - UI overhaul
